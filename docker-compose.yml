services:
  fastfetch-android-build:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        FASTFETCH_REF: dev
    image: fastfetch-android-builder:latest
    volumes:
      - ./out:/out
      - ./build-fastfetch-android.sh:/work/build-fastfetch-android.sh:ro
      - ./src:/work/src:ro
    environment:
      - JOBS=
      - OUT_DIR=/out
      # all | modern | legacy  (which architecture tier(s) to build)
      - TARGET_SET=all
      # optional comma list to restrict, e.g. armv7,armv8
      - BUILD_TARGETS=
    command: ["/bin/bash", "/work/build-fastfetch-android.sh"]
